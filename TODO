+truncate values for interpacket gap
+add some way of specifying normal distribution to -R
+-h e (examples)
+investigate -H x=localhost vs. x=127.0.0.1
+simplify arbitrary socket options
+fix crash in conjunction with newer libxmlrpc-c
+do something with UDP
+xml style output
+allow send volume to be specified (instead of duration/rate)
+meet more the spirit of IPPM (connectivity test, etc.)
+Add optional columns for sender/receiver buffer
+Add flowplotter to tarball?
+Add tcpi_total_retrans as column?
