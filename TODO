+make compatible with recent xmlrpc-c versions
+make it easier to define arbitary socket options
+remove I2-TSC and FASTTIME
+recover UDP option
+make traffic ports configureable (again)
+xml style output
+(try to)track route changes (IP route record option)
+allow send volume to be specified (instead of duration/rate)
+meet more the spirit of IPPM (connectivity test, etc.)
+finish automatic traffic capture option
+improve errorhandling of trafgen parser in case , instead of : (common handler)
